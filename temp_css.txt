/* ===== Minimal Loading & Cascading Slide-up ===== */
.loading-screen-overlay {
  position: fixed;
  inset: 0;
  z-index: 50;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  background: rgba(248, 249, 252, 0.95);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
  color: #334155;
  transition: opacity 0.4s ease, visibility 0.4s ease;
}

.loading-screen-overlay.fade-out {
  opacity: 0;
  visibility: hidden;
}

.loading-screen-content {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}

.loading-screen-icon {
  width: 48px;
  height: 48px;
  color: #94a3b8;
  margin-bottom: 20px;
  animation: spin 1.2s cubic-bezier(0.4, 0, 0.2, 1) infinite;
}

.loading-screen-text {
  font-family: var(--font-heading);
  font-weight: 600;
  font-size: 1.05rem;
  color: #475569;
  letter-spacing: -0.02em;
  animation: pulse-loading 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
}

@keyframes spin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

@keyframes pulse-loading {
  0%, 100% { opacity: 1; transform: scale(1); }
  50% { opacity: 0.6; transform: scale(0.98); }
}

/* Cascading Slide Up */
.stagger-item {
  opacity: 0; /* starts hidden */
}

.reveal-content .stagger-1 {
  animation: slideUpFade 0.6s cubic-bezier(0.2, 0.8, 0.2, 1) 0.1s forwards;
}

.reveal-content .stagger-2 {
  animation: slideUpFade 0.6s cubic-bezier(0.2, 0.8, 0.2, 1) 0.2s forwards;
}

.reveal-content .stagger-3 {
  animation: slideUpFade 0.6s cubic-bezier(0.2, 0.8, 0.2, 1) 0.3s forwards;
}

@keyframes slideUpFade {
  0% {
    opacity: 0;
    transform: translateY(20px);
  }
  100% {
    opacity: 1;
    transform: translateY(0);
  }
}
